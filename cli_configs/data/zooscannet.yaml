# @package _global_
data:
  module:
    _target_: datasets.zooscannet.ZooScanNetDataModule
    name: ZooScanNet
    data_root_dir: /home/d246a/Documents/data/SynergyUnitDatasets/ZooScanNet
    batch_size: 32
    data_fraction: 0.1
    train_transforms:
      _target_: augmentation.policies.zooscannet.TrainTransform
    test_transforms:
      _target_: augmentation.policies.zooscannet.TestTransform

  num_classes: 116

model:
  task: 'Classification'
  subtask: 'multiclass'
  cifar_size: False
  input_channels: 3
  patch_size: [224, 224]
  input_shape: ${model.patch_size}

wandb:
  entity: synergy_unit
  project: ZooScanNet