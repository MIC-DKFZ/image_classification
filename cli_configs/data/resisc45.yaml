# @package _global_
data:
  module:
    _target_: datasets.resisc45.RESISC45DataModule
    name: RESISC45
    data_root_dir: /home/d246a/Documents/data/SynergyUnitDatasets/RESISC45
    batch_size: 32
    data_fraction: 0.1
    train_transforms:
      _target_: augmentation.policies.resisc45.TrainTransform
    test_transforms:
      _target_: augmentation.policies.resisc45.TestTransform

  num_classes: 45

model:
  task: 'Classification'
  subtask: 'multiclass'
  cifar_size: False
  input_channels: 3
  patch_size: [224, 224]
  input_shape: ${model.patch_size}

wandb:
  entity: synergy_unit
  project: RESISC45
