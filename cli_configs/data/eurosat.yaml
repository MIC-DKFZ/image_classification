# @package _global_
data:
  module:
    _target_: datasets.eurosat.EuroSATDataModule
    name: EuroSAT
    data_root_dir: /dkfz/cluster/gpu/data/OE0441/d246a
    batch_size: 8
    data_fraction: 1.0
    train_transforms: 
      _target_: augmentation.policies.synergy_unit_transforms.FlipRotateTransform
      image_size: 224  # 224, 448
      norm: imagenet
    test_transforms: 
      _target_: augmentation.policies.synergy_unit_transforms.TestTransform
      image_size: 224  # 224, 448
      norm: imagenet
    
  num_classes: 10

model:
  task: 'Classification'
  cifar_size: False