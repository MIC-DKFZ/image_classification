# @package _global_
data:
  module:
    _target_: datasets.flowers102.Flowers102DataModule
    name: Flowers102
    data_root_dir: /home/d246a/Documents/data/SynergyUnitDatasets/Flowers102
    batch_size: 32
    data_fraction: 0.1
    train_transforms:
      _target_: augmentation.policies.flowers102.TrainTransform
    test_transforms:
      _target_: augmentation.policies.flowers102.TestTransform

  num_classes: 102

model:
  task: 'Classification'
  subtask: 'multiclass'
  cifar_size: False
  input_channels: 3
  patch_size: [224, 224]
  input_shape: ${model.patch_size}

wandb:
  entity: synergy_unit
  project: Flowers102
